name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Move tests
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7.0.1

      # `nft_gate` depends on access-gate-sui by local path (../access-gate-sui).
      # actions/checkout cannot place a repo outside the workspace, so clone the
      # sibling dependency manually next to this checkout.
      - name: Fetch access-gate-sui (local path dependency)
        run: git clone --depth 1 https://github.com/meddleware-org/access-gate-sui.git "${GITHUB_WORKSPACE}/../access-gate-sui"

      - name: Install suiup
        run: |
          curl -sSfL https://raw.githubusercontent.com/MystenLabs/suiup/main/install.sh | bash
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"

      - name: Install Sui CLI (testnet channel)
        run: suiup install sui@testnet-v1.76.1

      - name: Verify Sui version
        run: sui --version

      - name: Build and test
        run: sui move test --build-env testnet
